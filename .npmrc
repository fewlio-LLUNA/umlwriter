# Cloudflare worker 専用ツール（wrangler / partyserver）が要求する
# @cloudflare/workers-types の peer バージョンが v5 と v4 で両立しないため、
# peer 衝突を無視してインストールする。これらは worker の型付け専用で、
# Vercel がビルドする Next アプリ本体には影響しない。
legacy-peer-deps=true
